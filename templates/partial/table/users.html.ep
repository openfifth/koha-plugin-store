<table class="table">
  <thead>
    <tr>
      <th>ID</th>
      <th>Username</th>
      <th>Email</th>
      <th>Password</th>
    </td>
  </thead>
  <tbody>
    % for my $user (@{ $users }) {
    <tr>
      %= t td => $user->id
      %= t td => $user->username
      %= t td => $user->email
      %= t td => $user->password
    </tr>
  % }
  </tbody>
</table>

